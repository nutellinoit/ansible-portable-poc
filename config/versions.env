# Single source of truth for all version pins.
# Sourced by build.sh and the GitHub Actions workflow.

# python-build-standalone release tag and CPython version.
# Assets: https://github.com/astral-sh/python-build-standalone/releases
PBS_RELEASE="20260610"
PYTHON_VERSION="3.12.13"

# ansible-core pinned version (installed via pip into the bundle).
ANSIBLE_CORE_VERSION="2.21.0"

# Collections are pinned in requirements.yml (ansible.posix etc.).
